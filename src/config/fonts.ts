import { Inter,Montserrat_Alternates, Roboto,Open_Sans } from "next/font/google";


export const inter = Inter({ subsets: ["latin"] });

export const titleFont = Montserrat_Alternates({ subsets: ["latin"],weight:['500','700'] });

export const roboto = Roboto({ subsets: ["latin"], weight: ['400', '700'] });

export const openSans = Open_Sans({ subsets: ['latin'], weight: ['400', '600'] });